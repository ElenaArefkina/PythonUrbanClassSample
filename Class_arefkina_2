class Cat:
      """
      Создание и подготовка к работе объекта "Кот"
      :param weight: Вес кота
      :param age: Возраст кота
      :param background: Порода/происхождение кота
      """
    def __init__(self, weight: int, age: int, background: str):
      self.weight = weight
      self.age = age
      self.background = background

    def home(self) -> bool:
      """
      Функция, которая проверяет заберут ли кота.
      Если возраст котенка до месяца, то заберут.
      :return: Заберут или не заберут
      """

    def character(self) -> str:
      """
      Функция, которая проверяет способен ли кот к обучению командам.
      Если кот породистый, то способен.
      
      :return: Способность к обучению
      """
    
    def weight_func(self) -> bool:
      """
      Функция, которая проверяет возможно ли взять кота с собой в самолет.
      Если вес кота до 8 кг, то можно.
      :return: Можно ли взять в самолет
      """
