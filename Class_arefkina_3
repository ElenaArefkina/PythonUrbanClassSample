class Ring:
      """
      Создание и подготовка к работе объекта "Кольцо"
      :param size: Размер кольца
      :param material: Материал кольца
      :param price: Стоимость кольца
      """
    def __init__(self, size: int, material: str, price: int):
      self.size = size
      self.material = material
      self.price = price

    def size_func(self, size_finger) -> int:
      """
      Функция, которая проверяет насколько подходит/
      не подходит кольцо по размеру.
      Если размер не подходит возвращается значение разницы между размером 
      кольца и пальца (положительное или отрицательное).
      :param size_finger: Размер пальца
      :return: Значение разницы размеров
      """

    def material_func(self, material_bracelet) -> bool:
      """
      Функция, которая проверяет подойдет ли кольцо к браслету.
      Если материалы совпадают, то подойдет.
      :param material_bracelet: Материал браслета
      :return: Подойдет ли кольцо к браслету
      """
    
    def buy(self, right_price) -> int:
      """
      Покупка кольца.
      Если стоимость кольца превышает стоимость, подходящую покупателю, 
      то возвращается значение, на которое превышена цена
      :param right_price: Подходящая цена
      :return: Разница цен
      """
